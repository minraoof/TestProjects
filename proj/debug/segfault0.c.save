#include <stdio.h>
#include <stdlib.

int main(int argc, char * argv [])
{
        int x, y;
        printf("&x = %p, &y = %p, &gvaluse = %p \n", &x, &y, &gvalue);
        mysub();
        printf(" press enter to continue!\n");
        fgetc(stdin);
        return 0;
}
